the function my_func is a boolean function that returns true if the children of a node are properly pointed to by the parent in a binary tree format meaning left would be less than the parent value and the right would be more.
